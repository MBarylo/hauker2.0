//import type PostComment from './PostComment'

export type PostType = {
  id: string;
  content: string;
  authorId: string;
  // likes: number
  // comments: PostComment[]
  // createdAt: number
};
